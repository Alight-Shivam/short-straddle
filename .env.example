# Copy to .env for local dev (Vite auto-loads .env files with the VITE_ prefix).
# Points the frontend at the backend from server/ — see server/.env.example
# for the other half of this pairing (FRONTEND_ORIGIN must match where this
# app is actually served from).
VITE_API_BASE_URL=http://localhost:4000
